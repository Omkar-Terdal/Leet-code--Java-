class Solution {
    public String reverseWords(String s) {

        String[] trimmed=s.trim().split("\\s+");

        String out="";

        for(int i=trimmed.length-1; i>=0; i--){
            out+=trimmed[i]+" ";
        }
        return out.trim();

    }
}
